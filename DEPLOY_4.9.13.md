# 4.9.13 发布记录

- 匈奴副本新增主线同位置的全军前进、后退按钮（570/650，y=0），使用副本自身的士兵和碰撞逻辑；前进不会自动让己方投石车开火，后退可移动投石车。
- 目标：`47.114.59.65:3000`，主机 `IZM2IOMVNEXNLDZ`，计划任务 `sgqz-online-4.9.0`。
- 发布后 `/api/version`：4.9.13，任务 Running；备份：`C:\sgqz\backup-before-fix-4.9.13`。
- 公网两个 SWF SHA256 均为 `25f0b6cacfe17e90fb178d60c7a7ca2044896459937b13cf8df87f59018ebaf4`，各 1,117,779 字节，内嵌版本均为 4.9.13。
- Flex 编译通过，仅有既有 `UI.as` 类型转换警告；临时部署密钥已移除。

