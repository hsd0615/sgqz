# 武将资源体积核对与无损优化（4.9.3）

## 为什么新增约 100 MB

Git 提交 014053c（v4.8.4，2026-08-13）将原 817,771 字节的矢量武将资源替换成扩展武将资源，并转入 Git LFS。Git 中显示的 134 字节是 LFS 指针，不是实际资源体积。本地与当前发布源 general.swf 的实际长度为 113,612,274 字节，SHA256 一致。

拆解全部 SWF 标签后发现：

- 原版不含 DefineBitsLossless2 位图，主要由矢量形状、动画和少量代码构成。
- 新版包含 75 张 DefineBitsLossless2 位图，共 113,209,945 字节。
- 其中 53 张大图占 112,901,892 字节，尺寸主要为 1920×1920、2048×2048、1584×2048，最大高度 2400。
- 另外 22 张小图仅约 0.31 MB；新增代码不是主要体积来源。
- 位图内部和 SWF 外层均已使用 zlib 压缩；只调整 zlib 压缩等级无法显著缩小。

## 本次优化

将适合优化的 DefineBitsLossless2 原始 ARGB 压缩数据转为 PNG，使用 Flash 10+ 支持的 DefineBitsJPEG2 PNG 图像编码，保持角色 ID 和图片尺寸。PNG 的行过滤比原始像素直接 zlib 更适合这些图像。

- 优化前：113,612,274 字节（113.61 MB）。
- 优化后：83,744,823 字节（83.74 MB）。
- 减少：29,867,451 字节，26.29%。
- 72 张图成功缩小，3 张收益不足的图保持原编码。
- 未删除武将、动画帧、类名、矢量形状；没有缩放图片或降低颜色精度。
- 优化产物 SHA256：02d4ab024e16d32502d6a69b67a0053b14247f9346605a09b2b9ce1840859a9e。

## 验证与限制

独立校验脚本检查了全部 1,406 个标签。未转换的标签逐字节一致；转换图像的 ID、尺寸、alpha 通道和按四舍五入还原的预乘颜色像素完全一致。编译版本为 4.9.3，网页版资源 URL 增加 v=4.9.3 以更新缓存。

这次减少的是传输和文件大小，图片解码后的像素数量未降低，因此不能承诺同等比例减少内存或动画解码时间。尚未在原生 Flash 中逐个武将进行交互回归。云端保留 C:\sgqz\backup-before-assets-4.9.3 以便回退。

继续大幅缩小通常需要降分辨率、有损编码，或将大图按需加载；本次未做这些改变。

复现：python tools/optimize-general.py 输入.swf 输出.swf；独立验证：python tools/verify-general.py 输入.swf 输出.swf。依赖 Pillow、NumPy。
