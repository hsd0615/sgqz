# 4.9.12 发布记录

- 目标：`47.114.59.65:3000`，主机 `IZM2IOMVNEXNLDZ`，计划任务 `sgqz-online-4.9.0`。
- 反叛敌军使用“反叛+武将名”，结算提示显示 3 次招降机会；翻牌和玩家数据使用武将本名。
- 修复张飞等反叛武将翻牌只带代码导致名称回退为“武将不存在”的问题；24 名候选均有本地模型资源。
- 发布后 `/api/version`：4.9.12，健康状态正常，任务 Running；备份：`C:\sgqz\backup-before-fix-4.9.12`。
- 公网两个 SWF SHA256 均为 `a20ccb71c5400fa9a4d07a65b3a36cdd68752299e6c42d260369299229ca1259`，各 1,117,139 字节，内嵌版本均为 4.9.12。
- Flex 编译通过，仅保留既有 `UI.as` 的 `int` 转 `Boolean` 警告；临时部署密钥已移除。
