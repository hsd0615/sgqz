# 4.9.14 发布记录

- 修复匈奴翻牌超级武将返回错误 ID：接口现在返回 `general_id`，避免获得张飞后后续操作提示“武将不存在”。
- 反叛敌军名称和结算提示使用“反叛+本名”，玩家获得后仍保存为本名。第三关刷出反叛将领的概率为 15%。
- 目标：`47.114.59.65:3000`；发布后版本 4.9.14，任务 Running；备份：`C:\sgqz\backup-before-fix-4.9.14`。
- 公网两个 SWF SHA256 均为 `7b3f0ab6ff9bc7e362f966caa8dcc19c11be21309590e3c4577f1db311e225cd`，各 1,117,779 字节，内嵌版本均为 4.9.14。
- Flex 编译通过，仅有既有 `UI.as` 类型转换警告；临时部署密钥已移除。
