# v4.9.7 广播审计与发布记录

## 广播清单

| 类型 | 触发条件与修复方式 |
| --- | --- |
| 超级、一流武将获得 | 普通招募、求贤令翻牌、副本超级招募、首通赠将成功后，服务端判断 title=0/1 并全服发送；失败不发，求贤令重复请求不重发 |
| 极品、彩色装备 | 实际入包且品质≥5；包含主线预计算掉落、兜底随机掉落、首通装备奖励、副本翻牌；品质10标为彩色；取消准备战斗时的“即将获得”广播 |
| 试炼之地通关 | 第9章，胜利后按 stage.xml 真实关卡名广播 |
| 敌袭通关 | 第10章，胜利后广播，失败不发 |
| 副本通关 | 袭杀匈奴/荡平倭寇第三关胜利 |
| 副本道具 | 翻牌非装备道具获得（保留原有副本通知）；普通装备不再刷全服通知 |
| 武将进化 | 成功进化至2级及以上 |
| 克制进阶 | 成功提升合法的三项克制之一 |
| 高级天赋 | 获得三级天赋 |
| 擂台开启 | 保留原有客户端事件，统一全服分发并包含发起玩家 |
| 攻擂、守擂 | 保留原有胜利、首守、连续守擂和20次守擂成功事件，走同一全服通知通道 |
| 管理员公告 | 使用统一公告持久化、全玩家分发通道 |

普通世界聊天和喇叭文本同样受益于公共传输修复。TextFactory.makeZhankuang / makeZhankuangException 只有模板定义，当前版本无调用点，不虚构不存在的触发条件。静态 paoma.xml 跑马灯与版本更新说明不属于玩家事件广播。

## 根因与修复

1. 服务端轮询队列超过10条就只保留最后5条，未读广播直接消失。改为保留一小时，按每玩家递增序号分批读取，每批最多100条，不按队列条数丢弃未读数据。
2. 客户端用第一个右方括号切割JSON；正文方括号、邻居数组等导致解析失败。服务端曾用字符串替换修改整个JSON，进一步破坏数组结构。现完整解析JSON，不改写结构。
3. 解析失败仍将接收时间推进到服务端/本地时间；本地时钟超前和同毫秒消息也会漏收。改为服务端序号游标，只有完整有效响应才推进，失败重试原游标。
4. 150毫秒启动一次轮询但没有在途锁，网络稍慢时重复并发、乱序覆盖。改为单请求在途，10秒超时关闭后重试；旧响应不再生效。
5. 系统通知依赖客户端回调发世界聊天，部分奖励路径根本不触发；预计算装备仅战前预告，实际领取不广播。奖励/成长/关卡通知移至服务端成功路径，旧客户端本地事件标记为服务端负责，避免新版双发。
6. 纯文本只送地图，其他界面看不到；并继续尝试旧ByteArray解码。现纯文本统一进入UI消息处理并停止错误二次解码，新增根级全局横幅，战斗和抽卡等面板上方可见。
7. 登录补发、轮询与TCP同时收到同一公告会重复显示。增加公告ID，客户端去重；登录/注册兼容路径均返回最近一小时公告。

## 验证

- tools/test-broadcasts.cjs：4个模拟收件人（本人、网页、桌面、离线），135条同毫秒突发消息分批完整接收；TCP转发、同游标重试、嵌套数组、方括号、引号、无效JSON不推进游标、奖励品质边界、重要关卡、真实副本翻牌处理和主线预计算掉落处理通过。失败战斗无广播，副本重复翻牌无重发。
- tools/test-recruit-policy.cjs：新增同一卡位重试不重复广播；既有抽卡概率、通关解锁、幂等奖励回归通过。
- army-factory、battle-cards、xiongnu-equipment 回归通过；Flex编译通过，保留已有 forceHp 转布尔警告。
- 在服务器运行实际部署文件内的广播/轮询函数，使用隔离内存和模拟TCP：3个模拟玩家各收到125条消息，TCP125条，未向真实玩家发送测试广播。
- 未运行原生Flash多个真实客户端的界面交互测试。横幅及AS3类型经编译检查，接收方法体经替身测试；不将此表述为真人在线实测。

## 部署

- 版本4.9.7，目标47.114.59.65:3000；实例i-bp1e0xihm2iomvnexnld，主机IZM2IOMVNEXNLDZ；仅重启sgqz-online-4.9.0。
- 备份C:\sgqz\backup-before-fix-4.9.7，包含发布文件与停服后的data\sanguo.json。发布失败自动恢复代码文件。
- 保留线上Windows和认证适配，仅移植本次改动。部署前验证4.9.6及服务端/SWF哈希。
- 线上/api/version=4.9.7；重新下载main.swf、sanguo_web.swf，SHA256一致：bd9cc30590f919481fdacb1019c0303b187f2de510f6dc6527efd22a6e9a8865，1116135字节；解压均含4.9.7。网页入口、version、XML哈希也一致。
- 玩家需要重新进入新版客户端/刷新网页；旧版仍含旧JSON接收器，不能保证旧进程消息完整性。
- 完整线上文件校验见DEPLOY_VERIFY_4.9.7.json。

隔离发布分支codex/restore-online-new-server；未覆盖主工作区其他未提交修改。
