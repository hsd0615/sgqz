# v4.9.5 战斗指令、武斗伤害和求贤令抽卡修复

## 已修复

1. 全军前进：选择敌方最前方的存活武将，排除死亡/空槽；全部我方武将持续执行攻击，目标死亡或前排改变后重新选择。投石车新增目标攻击入口，依据 StoneWeapon 的抛物线参数计算发射角度及力度。手动全军命令暂停自身自动AI，防止它覆盖目标。
2. 全军后退：不打断已经开始的攻击结算，在可切换时先 stand 取消原追击、移动和冷却完成回调，然后按阵营方向后移。修复近战追击移动结束时距离判断颠倒、空目标继续攻击，以及移动监听冲突。
3. 武斗兵伤害：此前创建的是 Saber，但 FIRE_COMPLETE 只给骑兵走近战分支。武斗兵事件没有 target，误走默认 Weapon 分支。现将武斗兵及部件近战兵纳入近战结算，双方统一按有效攻击距离进行伤害与闪避判断。
4. 抽卡：修复 maxFlips 的服务端/客户端字段位置不一致、相同奖励卡被当成同一卡、超级品质 title=0 被 ||3 覆盖，以及单令牌翻牌后的关闭路径。奖励由服务端在实际扣令翻牌时产生，牌局+卡位用于幂等奖励，客户端 result 不再决定奖品；网络未确认时可以重试原卡而不重复扣令。
5. 奖池：每次付费翻牌独立5%概率抽取已通关解锁且未拥有的超级武将，其他95%为 quality=1 的普通装备，不出现一/二/三流武将。无符合条件超级武将时100%普通装备。按姓名去重旧新编号，历史通关通过 finished_stages + stage.xml 奖励名单恢复解锁，包括多个武将用顿号分隔的关卡。

## 验证

- tools/test-army-factory.cjs：20条武斗兵配置×4进化等级及5个控制分支通过。
- tools/test-battle-cards.cjs：前排选择/死亡换目标/左右后退/武斗双方伤害和距离/投石车轨迹/相同装备奖励卡/超时重试/单令关闭通过。AS3方法体以显示与事件替身执行，Flex编译单独检查类型。
- tools/test-recruit-policy.cjs：5%边界与10000个分层概率样本（500次超级），通关解锁、品质0保留、普通装备限制、重复请求不再扣令、不同卡位相同奖励均可领取、拒绝伪造奖品和过期牌局均通过；同一测试也针对实际待部署Windows服务端执行通过。
- 实际XML校验：3_44通关解锁吕布；6_90解锁黄忠/关羽/张飞；8_110解锁许褚/典韦/夏侯惇。普通装备为铁剑、皮甲、木符、布帽、草鞋、铜戒指（含旧新编号）。
- Flex SWF编译通过；保留已有UI.as forceHp隐式布尔转换警告。武将数值HTML重新生成，185条。
- 未做原生Flash逐场景交互回归，不能将替身测试表述为真实客户端点击测试。

## 部署

- 服务器：47.114.59.65:3000；仅重启 sgqz-online-4.9.0 计划任务。
- 备份：C:\sgqz\backup-before-fix-4.9.5；上传与替换前校验线上4.9.4服务端/客户端哈希，部署失败自动回滚。
- 保留线上Windows路径和认证适配，仅移植本次求贤令处理、奖池函数及版本改动。
- /api/version：4.9.5。桌面main.swf和网页sanguo_web.swf均从线上重新下载，与本地编译产物一致，解压后含4.9.5。
- SWF长度：1115163字节；SHA256：27d74482d0578d93a4b4070728e53f2539eac199d44c996539329cccdc32351f。
- 网页入口缓存版本4.9.5；未登录抽卡/翻牌请求被拒绝；临时部署SSH公钥已安排移除。
- 本次发布源码在 codex/restore-online-new-server 分支。主工作区有独立未提交内容，未用发布分支整体覆盖。
